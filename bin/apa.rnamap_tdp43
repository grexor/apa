#!/usr/bin/python

import apa
import sys
import argparse
parser = argparse.ArgumentParser()
parser.add_argument('-comps', action="store", dest="comps_id", default=None)
args = parser.parse_args()
apa.rnamap_tdp43.process(args.comps_id)
